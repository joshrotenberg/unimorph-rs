# Custom build setup for cargo-dist
# Sets environment variables needed for building with PyO3 in the workspace
- name: Set PyO3 compatibility mode
  run: echo "PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1" >> $GITHUB_ENV
  shell: bash
